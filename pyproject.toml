[project]
name = "lightjunction"
version = "1.0.0"
description = "GitHub profile README automation"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
