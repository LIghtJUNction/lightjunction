# 🤖 AI 问答助手使用指南

## 简介

AI 问答助手使用 GitHub Copilot 的免费模型，为你提供即时的技术问答服务。只需创建一个 issue，AI 就会自动生成回答。

## 使用方法

### 1. 创建问题 Issue

访问 [Issues 页面](../../issues/new) 创建新 issue。

### 2. 添加标签

为 issue 添加 `qa` 标签。这会触发 AI 问答工作流。

### 3. 编写问题

在 issue 正文中清晰地描述你的问题。

**好的提问示例：**

```
标题：Python 中如何高效地读取大文件？

正文：
我需要处理一个 10GB 的文本文件，直接用 open() 读取会占用太多内存。
有什么更好的方法可以逐行处理大文件而不会内存溢出？
```

```
标题：JavaScript 的 Promise 和 async/await 有什么区别？

正文：
我在学习异步编程，看到有人用 Promise，有人用 async/await。
这两种方式的主要区别是什么？什么时候应该用哪一种？
```

### 4. 获得回答

提交 issue 后，工作流会：
1. 自动读取你的问题
2. 使用 GitHub Copilot 生成专业回答
3. 将问答内容更新到 README
4. 在 issue 中回复确认

## AI 助手特点

### 擅长回答

✅ **编程语言问题**
- Python, JavaScript, Java, C++ 等语法和特性
- 代码示例和最佳实践
- 常见错误和调试技巧

✅ **技术概念解释**
- 算法和数据结构
- 设计模式
- 软件架构

✅ **工具和框架使用**
- Git 操作
- Docker 容器
- Web 框架（React, Vue, Django 等）

✅ **开发实践**
- 代码优化建议
- 性能提升技巧
- 安全最佳实践

### 回答风格

- 🎯 **简洁明了**: 先给出直接答案
- 📝 **详细解释**: 提供必要的背景知识
- 💻 **代码示例**: 包含实用的代码片段
- 🔗 **相关资源**: 推荐学习资料（不编造）
- 🌏 **中文回答**: 使用中文解释，代码用英文

## 系统提示词

AI 助手使用以下系统提示词：

```
你是一个友好、专业的技术助手，专注于帮助开发者解答编程、软件开发和技术相关的问题。

你的特点：
1. 回答简洁明了，突出重点
2. 提供实用的代码示例（如果适用）
3. 使用中文回答，但代码和技术术语使用英文
4. 态度友好，鼓励学习
5. 如果不确定答案，会诚实说明
6. 优先推荐最佳实践和现代化的解决方案

回答格式：
- 先给出简短的直接答案
- 如果需要，提供详细解释
- 如果适用，给出代码示例
- 可以提供相关资源链接（但不要编造）

请用专业但友好的语气回答问题。
```

## 提问技巧

### ✅ 好的提问

**具体清晰**
```
❌ Python 怎么用？
✅ Python 中如何使用列表推导式过滤列表中的偶数？
```

**提供上下文**
```
❌ 我的代码不工作
✅ 我在使用 Flask 框架开发 API，但是遇到 CORS 错误，如何正确配置 CORS？
```

**一次一个问题**
```
❌ JavaScript、Python、Go 哪个最好？如何学习？有什么书推荐？
✅ 对于 Web 后端开发初学者，Python 的 Django 和 Flask 框架应该选择哪个？
```

### ❌ 避免的提问

- 太宽泛："编程怎么学？"
- 多个无关问题："Python 怎么读文件？Go 怎么并发？Java 怎么连数据库？"
- 需要实时信息："今天的 GitHub 热榜是什么？"
- 主观意见："哪个编程语言最好？"

## 注意事项

### 限制

- ⏰ AI 回答生成可能需要 10-30 秒
- 🌐 回答基于训练数据，可能不包含最新信息
- 🎯 适合技术问题，不适合需要实时数据的问题
- 🔍 复杂问题可能需要多轮交互

### 隐私

- 📝 所有问题和回答都是公开的
- 🔒 不要在问题中包含敏感信息（密码、密钥等）
- 👥 issue 内容对所有人可见

## 示例问答

### 示例 1：算法问题

**问题：**
```
如何用 Python 实现二分查找算法？
```

**AI 回答：**
```python
def binary_search(arr, target):
    """
    二分查找算法
    
    参数:
        arr: 已排序的列表
        target: 要查找的目标值
    
    返回:
        目标值的索引，如果不存在返回 -1
    """
    left, right = 0, len(arr) - 1
    
    while left <= right:
        mid = (left + right) // 2
        
        if arr[mid] == target:
            return mid
        elif arr[mid] < target:
            left = mid + 1
        else:
            right = mid - 1
    
    return -1

# 使用示例
numbers = [1, 3, 5, 7, 9, 11, 13]
result = binary_search(numbers, 7)
print(f"找到位置: {result}")  # 输出: 找到位置: 3
```

**关键点：**
- 时间复杂度 O(log n)
- 数组必须已排序
- 使用双指针缩小搜索范围
```

### 示例 2：工具使用

**问题：**
```
Git 中如何撤销最近一次提交但保留更改？
```

**AI 回答：**
```bash
# 方法 1: 软重置（保留更改在暂存区）
git reset --soft HEAD~1

# 方法 2: 混合重置（保留更改但不在暂存区）
git reset --mixed HEAD~1

# 方法 3: 仅撤销提交但保留所有更改
git reset HEAD~1
```

**说明：**
- `--soft`: 撤销提交，更改保留在暂存区（staged）
- `--mixed` (默认): 撤销提交和暂存，更改保留在工作目录
- `HEAD~1`: 表示回退到上一个提交

**注意：** 如果已经 push 到远程，需要使用 `git push -f`（谨慎使用）
```

## 手动触发

可以在 [Actions 页面](../../actions/workflows/qa_assistant.yml) 手动触发工作流，输入 issue 编号重新生成回答。

## 技术细节

- **模型**: gpt-4o-mini (通过 GitHub Models API)
- **最大回答长度**: 2000 tokens
- **温度参数**: 0.7 (平衡创造性和准确性)
- **响应时间**: 通常 10-30 秒

## 问题反馈

如果遇到问题（但不要添加 `qa` 标签），请创建 issue 描述：
- AI 回答不准确
- 工作流执行失败
- 其他技术问题
